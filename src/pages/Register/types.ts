export interface ILinkStyle {
    $variant?: string;
}

export interface IFormData {
    name: string;
    email: string;
    password: string;
}

export interface IUser {
    id: number;
    name: string;
    email: string;
    password: string;
}
